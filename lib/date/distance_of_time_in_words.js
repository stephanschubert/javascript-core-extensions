Date.prototype.distance_of_time_in_words = 
function(to_time, include_seconds, options) {

  // Default values

  to_time = to_time || 0;
  options = options || {};
  include_seconds = include_seconds || false;

  return "";
};